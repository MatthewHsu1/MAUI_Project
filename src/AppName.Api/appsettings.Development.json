{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "// NOTE": "Dev-only signing key for the local /api/auth/token stub. Not a production secret — production values must come from user-secrets/env and a real IdP must replace this endpoint before deployment.",
    "Issuer": "appname-api-dev",
    "Audience": "appname-clients-dev",
    "Key": "dev-only-signing-key-not-for-production-use-32chars-min"
  },
  "Cors": {
    "Origins": [ "http://localhost:5173" ]
  }
}
