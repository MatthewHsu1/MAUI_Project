{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "// NOTE": "Local dev Postgres only. Never put real/production credentials here — those come from user-secrets or environment variables (e.g. ConnectionStrings__AppDb).",
    "AppDb": "Host=localhost;Port=5432;Database=appname_dev;Username=appname_dev;Password=appname_dev"
  },
  "Jwt": {
    "// NOTE": "Dev-only signing key for the local /api/auth/token stub. Not a production secret — production values must come from user-secrets/env and a real IdP must replace this endpoint before deployment.",
    "Issuer": "appname-api-dev",
    "Audience": "appname-clients-dev",
    "Key": "dev-only-signing-key-not-for-production-use-32chars-min"
  },
  "Cors": {
    "Origins": [ "http://localhost:5173" ]
  }
}
